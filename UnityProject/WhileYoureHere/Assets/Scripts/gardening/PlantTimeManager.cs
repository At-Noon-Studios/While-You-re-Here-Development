using System.Collections.Generic;
using UnityEngine;

namespace gardening
{
    /*public class PlantTimeManager : MonoBehaviour
    {
        private List<PlantObject> plants = new List<PlantObject>();
    
        public static PlantTimeManager instance;

        private void Awake()
        {
            if (instance == null)
            {
                instance = this;
            }
        }

        public void RegisterPlant(PlantObject plant)
        {
            plants.Add(plant);
        }

        /*public void UnregisterPlant(PlantObject plant)
        {
            plants.Remove(plant);
        }#1#

        // Update is called once per frame
        void Update()
        {
            foreach (PlantObject plant in plants)
            {
                plant.WaterPlant();
            }

            //plants.RemoveAll(x => x.HasMaxStage());
        }
    }*/
}
